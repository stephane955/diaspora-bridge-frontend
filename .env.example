# DiasporaBridge — Supabase public client config (anon key only)
# Copy to .env and fill in values from the Supabase dashboard.
# NEVER commit .env with real keys.

# --- Development (required): staging only ---
EXPO_PUBLIC_SUPABASE_URL=https://tvorurbmzrpwvxwztpix.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_staging_anon_key_here

# --- Production EAS builds only (EAS_BUILD_PROFILE=production) ---
# EXPO_PUBLIC_SUPABASE_URL=https://xtyqcdktwxzuezarnqhz.supabase.co
# EXPO_PUBLIC_SUPABASE_ANON_KEY=your_production_anon_key_here

# Do NOT expose service role keys, DB passwords, or PSP secrets via EXPO_PUBLIC_*.
